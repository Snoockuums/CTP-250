//theString.find(str, n)
			//returns the position of str within the stringstarting the 
			//search at position n
//theString.substr(p,l) 
			// Returns a substring containing thecharacters starting at 
			// position p and spans l characters
